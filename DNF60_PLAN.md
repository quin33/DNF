# 《地下城与勇士·60 级经典版》世界观改造方案

> 目标实例：仓库根 `I:\DEEPSEEK\DNF`（实例名 DNF / 端口 8788 / 库 dnf.db / 站点前缀 `/dnf`）。
> 兄弟实例 `xiuxian/`（问道仙坊）与本方案**无关、不受影响、不改一行**。
> 范围：仅"世界观/题材层"整体替换 + 少量"词即规则"代码同步。不重写数值引擎、不动 DB schema。

---

## 〇、已定稿决策（用户拍板，2026-09-02）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 建角结构 | **5 大职业 + Lv10 转职**（复用现有 10 级突破门为转职；每职业 2~4 个子职业名 + 技能 flavor，数值机制不变） |
| 2 | 内容/等级跨度 | **首期 19 级 · 前三区域**（格兰之森→天空之城→天帷巨兽：洛兰→幽暗密林→雷鸣废墟→格拉卡→天空之城·龙人之塔→天空之城·黑暗玄廊→天帷巨兽·神殿外围）；扩到 60 级作二期，本次不做 |
| 3 | 站点三层名称 | 统一 **《地下城与勇士》**（index `<title>`、站内品牌、网关导航名），网关 prefix 仍 `/dnf` |
| 4 | 存量数据 | **清零测试数据并重种**：删 8 个 level-1 测试角色 + 移除已播种的旧修仙 AI 队友默认卡并重种 DNF 卡；65 个真实账号保留；操作前备份 dnf.db |
| 5 | 维护口径（2026-09-03） | **单机/离线模式不再维护**：本地引擎（localStorage 直接开本）保留为历史兜底，默认不跑、不接受功能修复；后续需求默认只改在线玩法（`server.js` + `game-engine.js` 权威副本），不要再重复说明 |

---

## 一、盘点结论（4 层并行只读盘点）

| 层 | 结论 |
|---|---|
| 现状定性 | 根实例当前题材 = **修仙（问道仙坊）**，其前身是深渊酒馆（`data.js` 仍是 `TAVARN_DATA`/`tavern` 全局名、`index.html` `<title>` 还是"深渊酒馆 S1"）。"DNF"只在基建层。 |
| 题材载体 | 根目录**无** world.md/lore 文档。世界观全部内嵌代码：`server.js:551-567` SYSTEM_PROMPT、`server.js:1795-1870` 一批结算型 prompt、`taixu-insight.js`、`data.js`、`game-engine.js` 的 `DUNGEON_POOL`、`game-create.js`、`ai-companions.js`、前端 `index.html`/`online.js` 文案。 |
| 真正的副本数据 | 不在 `data.js`，而在 `game-engine.js:29-100 DUNGEON_POOL`（7 图，每图 lore/enemies×6/bosses×2/loot×3，boss 带 `realm` 修为词）。`data.js` 只是同名的展示副本表 + 全文案。 |
| 美术成本 | **≈0**。无任何外链图/字体，全为 CSS 变量 + emoji（`style.css`）。换主题只改调色板与文案，不换图。 |
| 数据迁移 | **无需 DDL**。DB 表/列/角色 JSON key 全通用（characters 存 name+data TEXT），题材词只落在 JSON **value** 与文案里。存量仅 65 账号 / 8 个 level-1 测试角色 / 0 条日志，旧数据可显示性风险≈0。 |
| 最大风险点 | 不是文案，是 **"词即规则"耦合**：境界词被正则白名单校验、被按名字符串解析成数值、被当 `character_class` 回写存档。列表见第三节。 |
| 隔离确认 | 双实例只共享 users 账号表（镜像只读 + 网关统一登录），角色/日志/探险全隔离；本方案只动根文件。 |

题材词量级（根实例、不含 xiuxian）：酒馆 33 / 仙坊 53 / 修士 28 / 灵石 82 / 灵墟 44 / 探险 173 / 功法 85 / 筑基 54 / 灵根 32 / 练气 27 / 储物袋 20 … 前端单 `index.html` 约 500+ 处。

---

## 二、目标世界观底稿（阿拉德大陆 · 60 级经典时代）

一句话：玩家是**阿拉德大陆的冒险家**，在城镇**赫顿玛尔**接取**冒险家公会任务板**的差遣，组队进入**阿拉德地下城**刷图，AI 实时叙事，清怪 → 领主 → 翻牌/撤离 → 回城领金币，全程按 DNF 经典措辞与美术调性。

### 命名总表（建议值，具体字面在实现时可再调）

| 现主题 | 改为 | 备注 |
|---|---|---|
| 灵根（金木水火土） | **职业**：鬼剑士 / 格斗家 / 神枪手 / 魔法师 / 圣职者 | 建角唯一"出身"选择，原 5 灵根 → 5 职业 |
| 修为/境界（练气~元婴） | **职业 + 等级**（如 `Lv.7 鬼剑士`） | 敌人/首领同改用等级数值 + 品级词 |
| 宗门 / 宗门差遣 | 冒险家公会 / 任务板派单 | 开局接单、收尾回城结单 |
| 修士 / 道友 | 冒险家 | 成员/NPC 泛指 |
| 灵墟 | 地下城（格兰之森等） | 全语境 |
| 灵石 | **金币**（`gold` 字段名不变） | 货币解析正则同步 |
| 功法 / 术法（技能二分类） | **物理技 / 魔法技**（推荐；引擎只判两个不同字符串，必须保留二分类） | 功法(+2 探索)↔物理技；术法(+3 战斗)↔魔法技 |
| 黄玄地天阶（4 阶） | **普通 / 高级 / 稀有 / 神器**（4 阶，对应 DNF 白/蓝/紫/粉） | tier 白名单 + 颜色表同步 |
| 体魄/身法/神识/气运 | **力量 / 敏捷 / 智力 / 幸运** | `ATTR_NAME` + 卡片 + AI 结算 statBuff |
| 储物袋 | 背包 | |
| 闭关 / 突破 | 修炼 / 晋级（10 级瓶颈） | 若启用转职则为"转职"，见待拍板 |
| 参悟（太虚幻境） | 顿悟/觉醒 · 技能传承 | 机制 = AI 现场生成技能 JSON |
| 炼器（forge） | 锻造 / 凯丽强化 | AI 叙事换词 |
| 四维成长阶段 | 不变（机制） | |

### 站点三层名统一（当前 index title / 站内品牌 / 网关名 三方不一致）

统一为 **《地下城与勇士》**（index `<title>` / 站内品牌 / 网关导航名一致；网关 prefix 保持 `/dnf`）；副标题可用"·阿拉德（经典 60）"。

### 副本阶梯（`DUNGEON_POOL` 7 图 + `data.js` + 前端地图表同步换）

按当前 rank0~6、Lv≈1-19 的数值，映射到 DNF 经典低阶主线（顺序即难度）：

| rank | 现地图 | DNF60 地图（建议） | 区域 |
|---|---|---|---|
| 0 | 枯骨林[练气] | 洛兰 | 格兰之森 |
| 1 | 迷雾泽[练气-筑基] | 幽暗密林 | 格兰之森 |
| 2 | 赤炎谷[筑基] | 雷鸣废墟 | 格兰之森 |
| 3 | 万剑冢[筑基-金丹] | 格拉卡 | 格兰之森 |
| 4 | 幽冥渊[金丹] | 天空之城·龙人之塔 | 天空之城 |
| 5 | 雷音山[金丹-元婴] | 天空之城·黑暗玄廊 | 天空之城 |
| 6 | 落仙台[元婴+] | 天帷巨兽·神殿外围 | 天帷巨兽 |

- 每图重写 lore/explore/enemies/bosses/loot/reward（实现时逐图考据 60 版怪物与领主名，用各区域标志性怪物：哥布林、猫妖、牛头兵、龙人、石巨人、GBL 教徒……）。
- 现"特殊机缘/隐藏副本"位可放 **暗黑雷鸣废墟（隐藏僵尸图）** 或远古图（王的遗迹/悲鸣洞穴）作远景。
- 若二期要扩到 60 级，阶梯延续：暗黑城（蜘蛛/暗精灵墓地）→ 万年雪山（冰龙斯卡萨）→ 诺斯玛尔 → 远古/觉醒图，上限从 ~19 拉长。**本次不做，仅预留位。**

### 建筑 18 座（`data.js buildings`，`code` 锚点一律保留，只换 name/category/icon/description）

| 现名 | 建议 DNF 化 | 功能口径 |
|---|---|---|
| 丹药阁 | 炼金屋（诺顿） | 药水/消耗品 |
| 炼器坊 | 凯丽的强化屋 | 装备锻造/强化 |
| 灵植园 | 材料温室 | 原料产出 |
| 灵石矿场 | 泰拉石工坊 | 每日材料/金币产出 |
| 体修场 | 修炼场·力量特训 | 锻炼属性 |
| 身法阁 | 修炼场·身法特训 | 锻炼属性 |
| 藏经阁 | 导师大厅（技能商店） | 买/学技能书（现 478-512 商品表换 DNF 技能） |
| 聚灵台 | 修炼场（挂机修炼/晋级） | 现有闭关机制 |
| 太虚幻境 | 觉醒祭坛·顿悟 | AI 领悟新技能 |
| 灵茶楼 | 卡妮娜的茶点屋 | 请客/好感（现点酒机制） |
| 坊市 | 摊位街 | 商店 |
| 拍卖行 | 拍卖行 | 保留 |
| 任务堂 | 冒险家公会·任务板 | 悬赏（宗门差遣替代点） |
| 洞府宿舍 | 旅馆客房/仓库 | 休息/存取 |
| 疗伤阁 | 教会诊疗室（歌兰蒂斯） | 治疗 |
| 问心台 | 追忆之碑 | 阵亡冒险家纪念 |
| 灵签阁 | 命运占卜屋 | 每日运势/抽奖 |
| 秘境收藏馆 | 阿拉德图鉴馆 | 收集图鉴 |

### 其余词表

- **饮品 7** → DNF 消耗品 7（生命药剂/魔力药剂/朗姆酒/麦酒/肉串/干酪…），价格数值与 `key` 锚点不变。
- **材料 7**（data.js materials）→ 无色小晶块 / 炉岩碳 / 泰拉石 / 灵魂晶石 / 燃烧的心脏 / 哥布林的手骨 / 破碎的钢铁。
- **AI 队友 12 卡**（ai-companions.js DEFAULT_CARDS）→ 12 名 DNF 职业预设冒险家（职业+等级+装备+传记重写），`NPC_NAME_POOL` → 通用冒险家/阿拉德 NPC 名池；播种表 `ai_companion_cards` 里旧修仙默认行需删除重种。
- **NPC/角落卡**：赛丽亚、林纳斯、卡妮娜、凯丽、莎兰、风振、GSD、歌兰蒂斯、阿尔伯特、辛达、罗莉安、奥兰奶奶 等（导师对应 5 职业）。
- **主界面 Tab**：🍺酒馆→🏙️赫顿玛尔(城镇)、⚔️冒险者、🛡️小队、📜探险日志 等；座位 bar/table → 城镇广场/公会大厅招募席（座位机制保留，zone 锚点不动）。
- **状态词典**（index.html:77）：修炼中→训练中、突破中→晋级/转职中、参悟中→顿悟中、酒馆活动中→城镇活动中、已阵亡→阵亡。
- **主题色**：默认深蓝+琥珀酒馆色 → DNF 经典暗金/深褐调；`[data-theme=light]` 语义"白色仙侠风"注释与配色一并调整。

---

## 三、必须动的"词即规则"代码（一次性小改，非引擎重写）

这些点不换词就会：新词被白名单**拒绝**、被名字解析**静默丢加成**、或旧修仙词**永久残留**在存档/公开卡上。逐条列改法（行号会随编辑漂移，实现时按特征字符串检索）：

| # | 位置 | 现状 → 改法 |
|---|---|---|
| 1 | `server.js:317-323, 327, 359, 3237, 3241` `cultivationRealmForLevel` | 现状：level→"练气N层/筑基…"，升级/突破时**回写 `character_class`=境界**。改为：`character_class` 固定为**职业**（不随级改）；Lv10 突破门槛改为**转职**（如 鬼剑士→剑魂/狂战士/鬼泣/阿修罗），结算回写为转职子职业名，不再是境界词。 |
| 2 | `server.js:1888-1891` realm 正则白名单 `/^(练气…|筑基…)$/` | AI 返回的敌人修为校验。改为**等级数值夹紧**（如 1~19），不再校验境界词。 |
| 3 | `game-engine.js:106-116` `enemyRealmVal`/`actorRealmVal` | 现在把境界词按 10/13/16 分档解析成数值参与胜负修正。改为直接用等级数值/等级差，阈值数字保留。 |
| 4 | `game-engine.js:136-141, 615, 868-886` `rollEnemies`/boss realm/默认值 | 敌人写"等级"，boss `筑基初期`→`Lv.N 领主`；NPC 默认 `character_class='练气N层'`→职业、`objective` 照常引用副本名。 |
| 5 | `game-engine.js:12-18` `ATTR_NAME/QI_LAYER/SKILL_TIERS/SKILL_TIER_COLOR/BAD_TRAIT_POOL` | 词表替换：四维→力量/敏捷/智力/幸运；`QI_LAYER`（练气数字）整表删除或改"等级段"；tier 4 值→普通/高级/稀有/神器+颜色；负面特质可保留（偏通用）。 |
| 6 | `game-engine.js:124, 126-133` `traitBonus`/`ITEM_BONUS` | 按特质词正则、装备名子串给数值加成。新特质/新装备名必须**同步进这两张表**，否则新名无加成。（部分通用词如"寻宝/胆大心细/老练"可直接沿用。） |
| 7 | `game-engine.js:740-750` `parseLearnedSkills`；`game-engine.js:526-536` 技能二分类判定 | type/tier 白名单换成新枚举（物理技/魔法技 + 普通/高级/稀有/神器）；保留"两分类、战斗偏爱魔法、探索偏爱物理"的数值分支不动。 |
| 8 | `game-engine.js:778` `AI_PHASE_LABELS`；157-162 阶段 label | 入谷/探索/突破/归途 → 进入地下城/推进/深入/撤离回城 等；`callAIStory` 里 `stageLabel` 随之。 |
| 9 | `taixu-insight.js:3-4, 6-15, 33-47, 66-77` | `VALID_TYPES='功法|术法'`→`物理技|魔法技`；`VALID_TIERS`→4 稀有度；整段 `TAIXU_INSIGHT_SYSTEM_PROMPT` 修仙语料重写为 DNF 技能传承世界观；错误串"灵石不足"→"金币不足"。 |
| 10 | `game-engine.js:551-555` `extractGold`；`loot-settlement.js:43-98` | 货币从 AI 正文解析："N 块灵石"→"N 金币"（正则、计量词、`PSEUDO_LOOT_RE` 同步，函数名可留可改）。 |
| 11 | `server.js` 各处兜底叙事 | `646-647/2911`（快速回退）、`1987-1991`（死亡保底"气血耗尽陨落/灵墟"）、`888-890/1621/2386-2389`（炼器失败"法器"）、`2209/2230/2316/2604/3373`（缺省"灵墟/修士/无名修士"）→ DNF 措辞（冒险家/地下城/阵亡/装备/无名冒险家）。 |
| 12 | `db.js:1130-1170` `publicCharacterData` | 公开卡 root=traits[0]（现为灵根槽）→ 语义变职业位；老角色旧值随数据维护处理（见批次 E）。 |
| 13 | 状态/路由枚举（**不用改**，但要确认） | `status:'cultivating'/'breaking_through'/'insighting'`、`/api/.../cultivation/...`、`/api/.../taixu-insight/...` 均为英文机制枚举，**改名只在显示层**，URL 与字段保留，避免全链同步。 |

---

## 四、逐文件改动清单（按批次）

### 批次 A —— 数据与角色
- `data.js`：整份修仙文案重写为 DNF60（user 昵称/余额显示"金币"、tavern 品牌、公告、chat 开场、activity 论道大会→比武赛事、buildings 18 行、drinks、materials、matchmaking 7 图、npcs 启用 DNF NPC）；变量名 `TAVARN_DATA` 保留（改名工程大且无收益）。
- `game-create.js`：`ROOT_KEYS` 五行灵根 → `CLASS_KEYS` 5 职业；`ROOT_SKILLS` → 每职业 2 门初始技能（物理/魔法标签 + 稀有度）；`ITEM_CHOICES` → 每职业新手装备/消耗品；初始 `character_class`=职业、`traits[0]` 不再是灵根。
- `ai-companions.js`：`ITEM_LIBRARY`/技能词典同步换词；`DEFAULT_CARDS` 12 卡重写为 DNF 职业冒险家；`NPC_NAME_POOL` 换池。
- `taixu-insight.js`：枚举与 system prompt（见三.9）。
- `game-engine.js` **数据段** `DUNGEON_POOL`：7 图按第二节阶梯重写。

### 批次 B —— 引擎/结算词规则（第三节 1-12 的全部小改）
集中在 `server.js` / `game-engine.js` / `taixu-insight.js` / `loot-settlement.js`，字符特征检索、逐个替换，不动数值曲线与结算逻辑。

### 批次 C —— AI 叙事 prompt
- `server.js:551-567` SYSTEM_PROMPT：**整段重写**为 DNF 世界观底稿 + 术语表（职业/等级/金币/地下城/冒险家公会/任务板/赫顿玛尔/赛丽亚），文风规则同步。
- `server.js:569-639` `buildUserMessage`：成员行 `realm/root/功法术法` 字段 → 职业/等级/技能系别；`【此间生灵】【深处首领】` 字段随 DUNGEON_POOL；默认任务目标"完成宗门差遣"→"完成公会悬赏"；开局/收尾模板（626/627）改"接单出发 / 回城领赏"。
- `server.js:609` 突破叙事 → 晋级/转职叙事。
- `server.js:1795-1870` 结算型 prompt 一批（LOOT/SUMMARY/DEATH/OUTCOME/SCROLL/FORGE/TRAIT/LEARNED_SKILL/SETUP）：身份名、属性词、品级词、货币词、掉落实体（卷轴→技能书、法器→装备）全部按 DNF 重写；`SETUP_PROMPT` 的敌人修为区间 → 等级区间。
- `aiDecideSetup` user 拼装（1914）、`normalizeAiSetup`（见三.2）、快速回退叙事（见三.11）。
- 顺带：`index.html` 内 AI 叙事模板段（约 2696-3620，含战斗 crit 模板、阶段计划注释）→ 批量换 DNF 措辞。

### 批次 D —— 前端 UI/页面
- `index.html`：`<title>`、品牌页头、公告栏、主导航 Tab 与图标、状态词典(:77)、冒险家卡片字段（修为→职业+Lv、四维、灵石→金币、储物袋→背包、功法术法徽章→物理/魔法）、建角表单（灵根块 → 职业块，提交按钮"✨ 踏入仙途"→"出发，冒险家！"等）、藏经阁/聚灵台/太虚三个设施弹窗、座位与饮品、组队/匹配页、探险日志兜底词、副本地图数据表（realm→等级/领主）、login.html 标题与登录页文案。
- `online.js`：公开队/房间卡片、等待道友→等待冒险家、结算 toast/addFeed、突破成功文案、死亡弹窗。
- `admin.html` / `admin.js`：字段标签 境界/职业、灵石→金币、已学功法/功法池→技能。
- `site-nav.js` 与网关站点展示名（显示名，不改前缀 /dnf）。
- `style.css`：强调色变量与 light/dark 语义注释（换 DNF 经典暗金调）。

### 批次 E —— 测试与数据维护
- 同步断言含修仙枚举/文案的测试：`test/spirit-stone-settlement.test.js`（灵石正则→金币）、`test/loot-ownership-settlement.test.js:112`、`test/admin-api.test.js` 的 room_create（dungeon='迷雾泽'→新池名）、`test/app-shell.test.js:330`（功法/术法→新词）、`test/taixu-insight.test.js`（多处枚举/prompt）。跑全量回归确认通过。
- 数据维护：`ai_companion_cards` 中 `is_default=1` 旧修仙卡 **删除重种**（否则公开页显示修仙人设）；8 个 level-1 测试角色 **清零**；备份 `dnf.db` 后操作。

---

## 五、风险与隔离

1. **xiuxian 完全不受影响**：镜像只读、网关只读 users、角色/日志物理隔离；全程只改根文件。
2. **DB 无迁移**：无 DDL；唯一写库动作是删默认 AI 队友卡重种 + 可选的测试角色清零（都可回滚=先备份 dnf.db）。
3. **最大回归面在"词即规则"**：第三节清单漏一处就静默出 bug（新技能被拒/新装备无加成/AI 报的词被打回默认），所以批次 B 与 E（回归测试）必须同 PR。
4. **索引漂移**：上述行号仅作定位，实现按字符串检索，避免被前序编辑带偏。
5. **上线方式**：先跑本地/回退分支，确认无 `灵石/灵墟/练气/灵根/功法(残留)/仙坊/宗门` 等词残留后，再由统一控制台重启 DNF 进程（只重启 8788 那一个，不碰 xiuxian/网关）。

---

## 六、质量红线（验收用，grep 零容忍）

根实例（排除 `xiuxian/ node_modules/ .git/ docs/`）中出现以下任一即视为未完成：
`灵石 | 灵墟 | 练气 | 筑基 | 灵根 | 道友 | 仙坊 | 宗门 | 修士(泛指残留) | 藏经阁 | 聚灵台 | 太虚(文案残留) | 闭关 | 丹药 | 功法(残留) | 术法(残留) | 修为(残留) | 论道`

（提示：`taixu`/`TAVARN_DATA`/`gold`/`code` 等英文机制名与 URL 不在清单内，属有意保留。）

---

## 七、已定稿
1. 建角：**5 职业 + Lv10 转职**（详见第一节决策表 #1）。
2. 内容：**首期 19 级 · 前三区域**（格兰之森→天空之城→天帷巨兽），60 级作二期。
3. 站点名：**《地下城与勇士》**（三层统一，网关 prefix `/dnf` 不变）。
4. 存量：**清零测试角色 + 移除并重种旧修仙 AI 卡**，真实账号保留，先备份 dnf.db。
