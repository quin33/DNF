# 统一控制台使用说明

## 功能

统一控制台允许你在一个页面中同时管理「问道仙坊」和「DNF」两个游戏服务。

## 启动步骤

### 1. 先启动两个游戏的独立控制台

**问道仙坊控制台（端口 8790）**
```bash
cd I:/DEEPSEEK/tavern_clone
双击 启动控制台.bat
```

**DNF 控制台（端口 8791）**
```bash
cd I:/DEEPSEEK/DNF
双击 启动控制台.bat
```

### 2. 启动统一控制台

```bash
cd I:/DEEPSEEK/DNF
双击 启动统一控制台.bat
```

浏览器会自动打开 `http://127.0.0.1:8792`

## 功能说明

### 批量操作
- **启动全部服务** — 同时启动两个游戏的服务器
- **重启全部服务** — 同时重启两个游戏的服务器
- **停止全部服务** — 同时停止两个游戏的服务器
- **刷新全部状态** — 刷新两个控制台的状态显示

### 独立控制
每个游戏在统一控制台中都有独立的 iframe，显示各自的完整控制台界面：
- 服务器状态（运行中/已停止/进程PID/端口）
- AI 服务开关
- 运行日志
- 独立的启动/停止/重启按钮

## 端口分配

| 服务 | 游戏端口 | 控制台端口 |
|------|---------|-----------|
| 问道仙坊 | 8787 | 8790 |
| DNF | 8788 | 8791 |
| 统一控制台 | — | 8792 |

## 注意事项

1. **必须先启动两个独立控制台，再启动统一控制台。** 统一控制台只是聚合界面，不直接管理游戏服务，需要代理到 8790 和 8791。

2. **如果某个游戏的控制台未启动，统一界面中对应的 iframe 会显示错误提示。** 此时只能单独控制已启动的那个游戏。

3. **同一台机器上可以同时运行两个游戏。** 端口已完全隔离，不会冲突。

4. **批量操作是并发执行的。** 如果某个控制台未响应，不会阻塞另一个。成功的会正常执行，失败的会在 toast 提示中显示。

## 故障排查

### 问题：统一控制台显示"无法连接"

**原因：** 对应游戏的独立控制台未启动。

**解决：**
1. 确认两个控制台是否都在运行：
   ```bash
   netstat -ano | findstr "8790 8791"
   ```
2. 分别启动缺失的控制台

### 问题：端口 8792 被占用

**排查：**
```bash
netstat -ano | findstr :8792
```

**停止：**
```bash
taskkill /PID <PID> /T /F
```

## 文件说明

- `unified-control.html` — 统一控制台前端页面
- `unified-control-server.js` — 8792 端口的简单 HTTP 服务器
- `启动统一控制台.bat` — 一键启动脚本

## 架构

```
统一控制台 (8792)
├─ iframe: 问道仙坊控制台 (8790)
│   └─ 管理 问道仙坊游戏服务 (8787)
└─ iframe: DNF 控制台 (8791)
    └─ 管理 DNF 游戏服务 (8788)
```

批量操作通过 `fetch` 直接调用两个控制台的 REST API (`/api/control/server/{action}`)，不依赖 iframe 内部状态。
