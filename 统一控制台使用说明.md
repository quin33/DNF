# 统一控制台使用说明

一个页面、一个密码，同时管理「问道仙坊」和「DNF」两个游戏服务。

## 启动

双击 `启动统一控制台.bat`，浏览器会自动打开 `http://127.0.0.1:8792`。

不需要先启动别的东西。统一控制台自己会把两个游戏服务拉起来（可用 `UNIFIED_AUTO_START=false` 关掉这个行为）。

> 不要同时再运行旧的 `启动控制台.bat` —— 它会启动另一个 DNF 游戏服务，抢同一个 8788 端口。两者二选一。

## 登录

统一密码，取自环境变量，按以下顺序回退：

```
UNIFIED_PANEL_PASSWORD  →  CONTROL_PANEL_PASSWORD
```

也就是说，如果你已经在 `.env` 里设过 `CONTROL_PANEL_PASSWORD`，直接就能用，无需新增配置。想给统一控制台单独设一个密码时才需要 `UNIFIED_PANEL_PASSWORD`。

两个变量都是先看系统环境变量，再看本目录的 `.env`，所以 `$env:UNIFIED_PANEL_PASSWORD = '...'` 这种临时设法也有效，且优先于 `.env`。

两者都没设时进程会拒绝启动，不会裸奔。会话有效期 8 小时。

## 配置

全部集中在 `.env`，改完重启统一控制台生效。

### 面板自身

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `UNIFIED_PANEL_PORT` | `8792` | 面板端口 |
| `UNIFIED_PANEL_HOST` | `127.0.0.1` | 监听地址，保持本机 |
| `UNIFIED_PANEL_PASSWORD` | — | 登录密码，回退到 `CONTROL_PANEL_PASSWORD` |
| `UNIFIED_AUTO_START` | `true` | 启动时自动拉起两个游戏服务 |
| `UNIFIED_GAMES` | 见下 | 管哪几个游戏，JSON 数组 |

### 游戏列表

默认等价于：

```json
[
  { "id": "xiuxian", "name": "问道仙坊", "dir": "I:\\DEEPSEEK\\tavern_clone", "port": 8787 },
  { "id": "dnf",     "name": "DNF",      "dir": "I:\\DEEPSEEK\\DNF",          "port": 8788 }
]
```

要增删游戏或改路径，在 `.env` 里写一行 JSON（单行）：

```
UNIFIED_GAMES=[{"id":"xiuxian","name":"问道仙坊","dir":"I:\\DEEPSEEK\\tavern_clone","port":8787},{"id":"dnf","name":"DNF","dir":"I:\\DEEPSEEK\\DNF","port":8788}]
```

字段：`id`（唯一，用于 API 路径）、`name`（界面显示）、`dir`（游戏目录绝对路径）、`port`（游戏服务端口）。可选 `entry`，默认 `server.js`。

目录不存在的游戏会在界面上标成「目录不存在」并禁用按钮，不影响其他游戏。

## 功能

**批量** — 一键启动 / 重启 / 停止全部游戏服务。并发执行，某个失败不影响另一个，结果逐条回报。

**单游戏** — 每张卡片可独立启动 / 停止 / 重启，独立开关 AI，独立重载配置和连接自检。

**站点网关** — 卡片显示网关的运行状态、端口与上游路由（`/dnf/→8788` 这种），可单独启动 / 停止 / 重启。未开启（`UNIFIED_GATEWAY=false`）时整卡变暗、按钮禁用。

**状态** — 运行状态、进程 PID、端口、AI 开关与模型、配置是否完整，5 秒一刷。

**日志** — 按游戏筛选，或看「统一控制台」/「站点网关」自身的操作日志。日志里不含 API Key。

## 关于隔离

两个游戏服务是本进程的子进程，但各自用干净的环境变量启动，只注入 `PORT` 和它自己的 `cwd`。父进程里 DNF 的 `AI_API_KEY`、`TAVERN_DB_PATH` 之类不会漏进问道仙坊的子进程 —— 否则 `TAVERN_DB_PATH=./dnf.db` 这种相对路径会在对方目录下解析，让问道仙坊悄悄写进一个 `dnf.db`。每个子进程只读自己目录里的 `.env`。

关掉统一控制台会一并关掉两个游戏服务。

## 端口

| 服务 | 端口 | 说明 |
| --- | --- | --- |
| 问道仙坊 | 8787 | 由统一控制台管理 |
| DNF | 8788 | 由统一控制台管理 |
| 站点网关 | 8786 | 由统一控制台管理，`UNIFIED_GATEWAY=true` 时才启动 |
| 统一控制台 | 8792 | 仅本机 |

旧的独立控制台端口（8790 / 8791）在这套方案里用不到了。

## 站点网关

两个游戏对外挂在同一个域名的两个子路径下：

```
xiuxiangame.dpdns.org/          → 导航首页
xiuxiangame.dpdns.org/xiuxian/  → 8787
xiuxiangame.dpdns.org/dnf/      → 8788
```

网关（`site-gateway.js`）做的事就是剥掉 `/dnf` 这层前缀再转发，因此两个游戏的 `server.js` 不用知道自己挂在哪。cloudflared 的 ingress 不能重写路径，这一层只能由网关来做。

默认不启动。要开就在 `.env` 里设 `UNIFIED_GATEWAY=true`，端口和路由分别是 `GATEWAY_PORT`、`GATEWAY_SITES`。**cloudflared 的主域名已经指向 8786，所以线上必须开着，否则主域名 502。**

游戏页面左侧会有一个互跳导航条，窄屏（≤900px）自动改成顶部横排。它只在经网关访问时出现 —— 直连 8787 / 8788 时另一个游戏不同源，跳过去只会 404，所以干脆不显示。

## 故障排查

**端口 8792 被占用**

```bash
netstat -ano | findstr :8792
```

```bash
taskkill /PID <PID> /T /F
```

**启动时报「未设置密码」** — 在 `.env` 里加 `CONTROL_PANEL_PASSWORD=...` 或 `UNIFIED_PANEL_PASSWORD=...`。

**某个游戏一直起不来** — 展开那张卡片的日志看子进程的输出。常见原因是该目录下缺 `node_modules`（进去跑一次 `npm install`），或游戏端口已被别的进程占着。

**卡片显示「目录不存在」** — `UNIFIED_GAMES` 里的 `dir` 写错了，或者该游戏还没克隆到本机。

**主域名 502，但直连 8787 / 8788 正常** — 网关没起来。检查 `.env` 里 `UNIFIED_GATEWAY=true`，以及 `GATEWAY_PORT` 与 cloudflared ingress 里的端口是否一致。

**登录一个游戏后另一个掉线** — 两边的 localStorage 键撞了。同域名下 localStorage 按源共享，键名必须各带 `xiuxian_` / `dnf_` 前缀。

## 文件

- `unified-panel.js` — 统一控制台服务，含各游戏的 service-supervisor
- `unified-control.html` / `unified-control-ui.js` — 前端
- `site-gateway.js` — 站点网关，按路径前缀分发到各游戏
- `site-nav.js` — 游戏页面左侧的互跳导航（两个游戏各一份）
- `启动统一控制台.bat` — 一键启动

## 架构

```
unified-panel.js (8792)
├─ supervisor: 问道仙坊 → node server.js (8787)  cwd=tavern_clone
├─ supervisor: DNF      → node server.js (8788)  cwd=DNF
└─ supervisor: 站点网关  → node site-gateway.js (8786)   可选

cloudflared ──→ 8786 网关 ──┬─ /xiuxian/ → 8787
                            └─ /dnf/     → 8788
```

面板直接用 IPC 管子进程，不再经过 8790/8791，也没有 iframe。
